# Frontend dev server port
VITE_DEV_SERVER_PORT=3000

# Base path used by frontend requests (must match backend context-path)
VITE_API_BASE_PATH=/api

# Backend URL used by Vite proxy during local development
VITE_API_TARGET=http://localhost:8080
